# fi-maquetas

Hosting estático (GitHub Pages) de maquetas HTML generadas por Fi para revisión
de clientes cuando una llamada en vivo no es práctica. Cada tarea publica su
`mockup.html` en `<slug-tarea>/index.html`; el link no se lista en ningún
índice — solo se comparte con quien lo necesita.
